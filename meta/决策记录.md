# 知识库决策记录

本文记录影响目录、命名、边界和真源的历史裁决。新增决策使用 `D<number>` 编号，并包含日期、问题、决定和影响范围。

## 记录格式

```markdown
## Dxx — 标题（YYYY-MM-DD）
- 问题：
- 决定：
- 影响：
```

详细历史裁决目前保留在 `meta/内容目录与进度.md` 的原始记录中；后续新增裁决统一登记于本文。

## D11 — 模块2不建立逐干员人工页（2026-09-10）

- 问题：通用干员模板重复维护技能原文，并混入组合、替代和场景评价，和模块边界冲突。
- 决定：技能快照继续按设施分片；普通干员与技能查询视图由结构化数据和后续索引派生。删除通用 `tpl-干员.md`，改用 `tpl-特殊技能释义.md`，只覆盖快照与官方术语仍无法回答且高频误读的少数技能或机制。
- 影响：模块2不需要为每名干员建立正文；组合与替代归模块3，评价与场景建议归模块6，技能原文快照保持唯一事实来源。

## D12 — 干净语料目标与模块 README 去维护化（2026-09-10）

- 问题：模块1、模块2 的 README 把本地同步记录、变更纪事和「已知问题」写进了 `docs/`，而这些文档带 `status: canonical`、进 RAG，会把仓库维护过程当成玩家知识检索出来；同样内容在 `meta/内容目录与进度.md` 已有登记，构成重复真源。
- 决定：在《知识库模块设计》确立「干净语料库」目标——`docs/` 只保留当前有效的玩法知识，维护内容（同步/导入记录、变更纪事、已知问题、进度、待补、素材台账、写作规范、本地路径）一律只登记在 `meta/`。模块 README 降为 `type: index` 导航入口，只写职责与分区导航。
- 影响：`docs/1-基础设定/README.md`、`docs/2-干员技能/README.md` 已清理，被删段落的内容基本已在 `meta/内容目录与进度.md` 登记，仅「跑单/囤单建载体」一项补登为该文遗留待办 7，无信息丢失；同时清除了模块1 正文里「某日重订/整合素材 docx」式批注，并修掉 README 对不存在文件 `数据源.md` 的悬挂引用。root README 与 `meta/协作与引用规范.md` 同步声明该边界；模块3–7 README 的同类「现状/待补/素材出处」段落按同一规则逐步下沉。

## D13 — 模块 README 全面去维护化与 front matter 统一（2026-09-10）

- 问题：模块3–7 的 README 仍带 `现状`/`待补内容`/`素材出处` 与裁决编号；同一批正文里还有「某日裁决/旧名重命名/guides 引入/来源抓取于某日」等过程叙事；front matter 字段各行其是（`tags`/`domain`/`prts_ref`/`last_updated`/`last_reviewed`/`imported` 与 `type`/`status`/`source`/`updated` 并存，模板里还有重复的 `source` 键）。
- 决定：模块 README 一律降为 `type: index`，只写职责与文档导航；正文删除迁移、裁决、待办、来源抓取日期等过程叙事，缺口改由 `meta/内容目录与进度.md` 承载。front matter 统一为 `type`/`status`/`source`/`updated`：`prts_ref` 并入 `source`，`last_updated`/`last_reviewed`/`imported` 归并为 `updated`，废弃 `tags`/`domain`；模块3 关系字段（`system_id`/`operators`/`exclusive_with`/`coexists_with`/`shortcut_id`/`related_systems`）保留并在规范中登记。
- 影响：`docs/` 下 48 篇正文与 7 个模块 README 的 front matter 已统一；`meta/协作与引用规范.md` 与两个模板同步改写。`buff叠加模型.md` 因九步次序仍有未裁决分歧，标记 `status: draft`。组合注册表内的练卡展示口径拆分登记为 `meta/内容目录与进度.md` 遗留待办 9。

## D14 — 模块2 来源指向 RIIC-Web；组合注册表移除 ≤10 名额（2026-09-10）

- 问题：模块2 README 的 `source` 指向中转仓库 RIIC-KnowledgeAgent，而非真正的数据来源；`组合注册表.md` 混有玩家报告展示的「共享成员默认只占一条 ≤10 名额」规则。
- 决定：模块2 及其入口的来源统一指向 `KnightCodeSquareMatrix/RIIC-Web`，全量真源、清洗过程与刷新口径以 `meta/mirror/RIIC-Web/` 为准（`docs/2-干员技能/` 为派生只读快照）。组合注册表删除 ≤10 名额条款，其余展示/名额规则的存废待用户裁决。
- 影响：`docs/2-干员技能/README.md`、根 `README.md`、`meta/内容目录与进度.md` 的来源表述已更新；RIIC-KnowledgeAgent 仅作为 guides 系列（模块3/6）与血缘记录的来源保留，不再作为模块2 数据来源。

## D15 — 删除加工站「概率叠加口径未裁决」（2026-09-10）

- 问题：`加工站机制.md` 在「常见边界情况」和「相关机制」里挂了一条「概率叠加口径未裁决」，指向 [[buff叠加模型]]。核对后确认：加工站进驻上限为 1，不存在跨干员的概率叠加；各技能描述内的分档数值是技能原文自身口径，与 [[buff叠加模型]] 的九步结算次序是两回事，属于写法混同。
- 决定：删除该条与对应的相关机制链接，加工站不再声称存在未裁决的概率叠加口径。
- 影响：`buff叠加模型.md` 的争议仍只归其自身的结算次序问题（见遗留待办 3）；`训练室机制.md` 对 [[buff叠加模型]] 的引用未动，因其指向的是教官多条速度技能的真实叠加口径。

## D16 — 模块2 快照全量自动渲染；targets/efficiency 取消（2026-09-10）

- 问题：`docs/2-干员技能/` Markdown 快照的全量生成依赖私有上游项目（RIIC-KnowledgeAgent 的 build_refs.py 等），本库 `sync-skill-snapshot.mjs` 只能增补不能重写，且存在结构性盲区（「技能 → 持有者」节从不更新、三段技能链的替换前驱取错）与补源依赖（RhodeLogisticsSteward 的 targets/efficiency 字段，公开不可得）。
- 决定：
  1. 新增 `scripts/render-skill-snapshot.mjs`，从 `meta/mirror/RIIC-Web/clean/` 全量渲染快照；渲染口径——**targets 由官方标签（tags）替代**（「作用产物/作用职业」尾注一律不渲染，「通用生产」等标签不展开产物枚举），**efficiency 取消**（快照与数据源中本就零出现）；**技能 id 括注全部去除**（同名不同 id 靠提升口径与描述数值自然区分，全库仅 3 例同名提升链、零冲突）；**「技能 → 持有者」反向索引节删除**（按干员查走名册、同效走等价组，检索冗余）；**termRefs 以「术语」子行内联展开**（替代「引用术语」尾注，条目自包含）。
  2. `歧义.md` 拆分为 `歧义-子串对.md`（render 生成，31 组，短名≥2 字）＋`歧义-简称合称.md`（人工维护，含官方同名组）；新增 `docs/3-组合库/标签速查.md`（44 标签功能查表，按设施分节，render 生成，真源在模块2 数据）。
  3. `sync-skill-snapshot.mjs` 退役（格式与 render 产物不兼容，文件头已标注）；`verify-skill-snapshot.mjs` 保留为可选数据级校验。刷新流程改为「clean → render → git diff 过目」，见 `meta/mirror/RIIC-Web/维护更新说明.md` §4。
  4. 类别.md 全局资源计数 (N) 改为提供者干员数（存量「外势(3)/实地(2)」两处上游私有口径无数据可复现）；快照头部清理技术细节（数据源文件名、库内路径）与维护叙事。
- 影响：`docs/2-干员技能/` 13 篇全部重渲染替换（相对旧快照：400 处补源尾注消失、约 40 行新增术语引用、7 处按技能节新干员行随节删除、名册约 19 行设施序统一为 ROOM_ORDER、17 处空行错位与 1 处行尾全角空格清理）；模块3 新增标签速查；快照不再依赖上游 RIIC-KnowledgeAgent，数据链闭环于本库。
