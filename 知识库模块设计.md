# 知识库模块设计

> 本文是知识库的结构治理真源：定义模块边界、归属判断和依赖关系。详细文件清单、完成进度和历史裁决见 `meta/内容目录与进度.md` 与 `meta/决策记录.md`。

## 总体结构

知识库采用“7 个内容模块 + 1 个外部边界”：

| 模块 | 职责 | 目录 |
|---|---|---|
| 1 基础设定层 | 底层机制、设施规则与资源事实 | `docs/1-基础设定/` |
| 2 干员技能层 | 基建技能原文与单技能机制 | `docs/2-干员技能/` |
| 3 组合库 | 干员组合、替代、互斥与体系关系 | `docs/3-组合库/` |
| 4 排班算法层 | 排班求解的业务规则与输入输出 | `docs/4-排班算法/` |
| 5 换班操作层 | 游戏内换班和轮换动作 | `docs/5-换班操作/` |
| 6 专家经验层 | 场景判断、偏好和经验性建议 | `docs/6-专家经验/` |
| 7 布局查表层 | 布局定义与产出倾向 | `docs/7-基建布局/` |

依赖方向为：

```text
1 事实 → 2 技能 → 3 组合 → 4 算法 → 5 操作 → 6 决策
       └──────────────────────────────→ 7 布局查表
```

## 语料洁净目标

知识库的交付物是一份**干净的玩家知识语料**：检索或问答命中任何一段，都应当是在回答基建玩法问题，而不是在读这个仓库的维护过程。

- 语料正文只保留当前有效的事实、机制、组合、操作与经验；一段话若放进问答会暴露工程细节或流程信息，它就不属于语料。
- 不落语料的内容包括：同步/导入记录、变更纪事与「某日重订」叙事、已知问题与待裁决、待写/待补清单、完成进度、素材出处台账、写作规范、脚本与 commit 说明、本地磁盘路径、协作分工。这些统一登记在 `meta/` 治理文档。
- 模块 README 只作模块入口，写清「本模块职责 + 分区导航 + 阅读顺序」，不承载上述维护内容，也不写前向的进度承诺。
- 版本叙事不落语料：不写「自 xx 版本起」「截至 xx 版本」「xx 已于某日重订」；时效由 front matter `updated` 与治理文档承担（PRTS 概率数据自带的「更新于」日期可保留）。
- 只读快照（`docs/2-干员技能/`、`meta/mirror/`）按其来源原样保留，不套用上述改写。

## 归属原则

- 同一事实只保留一个主要维护位置，其他文档通过链接引用。
- 设施面板、生产规则、资源常数和基础机制属于模块 1。
- 单个干员或技能的原文与运作原理属于模块 2；模块 2 不维护组合归属和培养评价。
- 干员之间的组合、替代、互斥、资源流和组合数值锚点属于模块 3。
- 算法如何生成排班表属于模块 4；具体操作步骤属于模块 5；为什么选择某方案属于模块 6。
- 布局配比和产出倾向属于模块 7；选择理由属于模块 6，体系适配细节属于模块 3。
- 用户 box、需求、排班报告和其他运行时输入不进入长期知识库。

## RAG 边界

- `docs/**/*.md` 中的正文是知识语料候选；各模块 `README.md` 是 `type: index` 的导航入口，只用于模块路由，不承载知识正文。
- `docs/2-干员技能/` 的快照按治理文档登记的维护规则处理，不手改。
- 维护过程、进度、裁决、同步记录与本地环境信息一律登记在 `meta/`，不进入正文语料。
- `meta/`、`external/`、镜像目录、治理文档和 TODO 不进入正文语料。
- 文档清单、RAG manifest、向量索引、实体索引、关系图和查询路由表都是派生物，不是事实来源。

## 新增文档流程

1. 查本文判断模块归属，并查现有文档确认是否已有真源。
2. 选择合适模板和最小 front matter。
3. 明确来源、条件、边界，使用稳定 Markdown 链接引用其他模块。
4. 迁移或重命名时保留兼容入口，并在决策记录中登记。
5. 不为未来图谱、向量化或索引提前制造重复数据结构。
6. 模块 README 只增补职责与导航，不写进度、同步记录、变更纪事或已知问题。

## 配套文档

- `meta/内容目录与进度.md`：详细文件目录、完成状态和待补内容。
- `meta/决策记录.md`：历史裁决、迁移和命名决策。
- `meta/协作与引用规范.md`：多 Agent 编写和引用规则。
