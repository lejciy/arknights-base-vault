# 排班算法（模块4）

职责：说明排班表生成算法的工作机制（业务层面，非代码实现），用于回答「这个表是用什么规则生成的」。

## 待补内容

- 算法总览：输入（operbox / 布局 / 资源倾向）→ 求解流水线（中枢 → 发电 → 办公室 → 跨设施编排 → 各房搜索 → 轮换分配）→ 输出（排班表结构）的业务描述
- 轮换模型统一归纳：12/6/6、αβγ ABC、二班中枢 12/12、歌蕾蒂娅 7/5/7/5 特殊四班
- infra-cli 求解器的说明（原 `5-cli求解器/` 预留位并入本目录）

## 事实底料

求解器实现规范镜像位于 `meta/mirror/ArknightsInfraCalc/`（7 篇，只读、不具裁决权、**不入 RAG**）——效率模型、无人机结算、EffectAtom 设计、中枢排班规则、已建模干员、贸易产出锚点。业务层说明写作时从中引用事实，不复制实现词汇。

原 `docs/1-基建产出基准/` 目录已由该镜像区取代（2026-09-08 裁决 D5）。
