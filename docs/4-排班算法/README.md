---
type: index
status: canonical
updated: 2026-09-10
---

# 排班算法（模块4）

职责：说明排班表生成算法的工作机制（业务层面，非代码实现），用于回答「这个表是用什么规则生成的」——输入（operbox / 布局 / 资源倾向）→ 求解流水线（中枢 → 发电 → 办公室 → 跨设施编排 → 各房搜索 → 轮换分配）→ 输出（排班表结构），以及 12/6/6、αβγ ABC、二班中枢 12/12、歌蕾蒂娅 7/5/7/5 等轮换模型。

正文尚未落地。求解器实现规范镜像位于 `meta/mirror/ArknightsInfraCalc/`（只读、不具裁决权、不入 RAG），业务层说明写作时从中引用事实，不复制实现词汇。
