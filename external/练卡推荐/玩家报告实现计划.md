# 练卡推荐 · 玩家报告实现计划

> 文档角色：current-reference
> 生命周期状态：current
> 当前真源：docs/练卡推荐相关/README.md
> 摘要：把确定性玩家练卡数据与未来 AI 自然语言报告分层落地的工程计划

## 0. 目标与非目标

### 目标

给定 `operbox`（可选布局/效率上下文），先输出符合《玩家输出口径》的三块确定性数据：全量未完成基础名单、17 组组合完成度、去重后最多 10 人的行动建议。未来 AI 只消费这份结构化事实并生成五章自然语言报告，不在当前 builder 中生成文案。

### 非目标

| 禁止 | 原因 |
|---|---|
| 修改 `search/**`、`schedule/**` 的求解策略或主路径 solver 选型 | 练卡建议只消费既有 operbox、布局和效率事实；允许在排班报告中补充计算该事实所需的只读人数 |
| 玩家角色写入 Plan hard admission | 产品边界 |
| 用旧 `training_recommendations.json` 当玩家真源 | 已归档 v2 合同 |
| 凯尔希办公室机制 / 五章 AI 渲染 | 另 TODO |

### 成功标准（直接访问）

1. `incomplete_newbie` 保留全量未完成事实，T9 只改变章节状态和行动候选。
2. `combinations` 固定输出 17 组规范名、完成状态与逐成员持有/练度状态。
3. `recommendations` 去重且不超过 10 人，包含目标练度、主理由和稳定优先级。
4. 布局/效率上下文只触发已裁决条件，不从未知事实猜测效率。
5. 旧诊断 JSON 继续兼容，但不决定玩家语义或未来 AI 文案。

---

## 1. 并行隔离（白名单）

```text
crates/infra-core/src/training_advice/**
crates/infra-cli/src/commands/advice.rs
crates/infra-cli/src/commands/serve.rs              # plan.compute 上下文适配
crates/infra-cli/tests/advice_cli.rs
crates/infra-core/src/schedule/base_rotation.rs     # 只透传逐站当班人数
crates/infra-core/src/schedule/team_rotation.rs     # 只透传续段当班人数
data/training_advice_knowledge.json          # 新建
data/fixtures/training_advice/**
docs/练卡推荐相关/**
```

只读：`OperBox`、`OperatorInstances`、`PromotionTier`。推荐独立 worktree；勿提交他 agent 的 `baked`/`tier` 脏文件。

---

## 2. 架构

```text
docs/练卡推荐相关/*  →  training_advice_knowledge.json
                      →  player::build_player_report
                      →  advice --player / plan.compute.training_advice
                      →  未来 AI render 五章（未实现）
旧 evaluate + training_recommendations.json  →  diagnose 兼容
```

可选 `PlayerAdviceContext`：布局可确定的源石线、会客室、宿舍、工程机器人，以及调用方确认的贸易/制造人均效率。`advice` 内不重跑全量 search，也不从总产出猜测人均效率。

---

## 3. 阶段

| 阶段 | 状态 | 内容 |
|---|---|---|
| A | 已完成 | knowledge schema + 加载校验 + 最小 JSON |
| B | 已完成 | 前端结构化 player 输出 + `advice --player` + `plan.compute.training_advice` + 夹具；五章报告/`--answer` 留给未来 AI |
| C | 已完成 | 已批组合成员/目标/分支、C2 边界、动态技能成员、完成状态和散件条件知识 |
| D | 方案保留 | 旧 v2 CLI 诊断路径保留，但新前端直接使用 `plan.compute.training_advice`，不做旧输出兼容 |
| E | 已完成 | `PlayerAdviceContext` + `--player-context`；接入 C3、T9 和布局可判定散件条件 |
| F | 部分完成 | README/current-reference 已同步当前实现；随未来 AI 报告入口继续收尾 |

**当前结构化 MVP：** newbie 全表 + 17 组完成度 + 18 名散件知识 + ≤10 行动建议 + 布局/效率上下文。五章文案属于未来 AI 阶段。

“部分可用/30% 合格形态”规则已废弃，不再实现或写入输出。

---

## 4. 验证（实施时）

```bash
cargo test -p infra-core training_advice
cargo test -p infra-cli --test advice_cli
cargo run -p infra-cli -- advice --operbox data/fixtures/training_advice/... --player --layout data/layout/243.json
```

---

## 5. 授权门禁

本文是计划，不是实施授权。阶段 A/B/C/E 已由用户分别授权并实现；D 仍只讨论方案，五章自然语言报告和 `--answer` 仍未授权实现，留给未来 AI 接入。

## 6. C/E 后仍开放的知识与上下文项

- 水月目标已确定为精二；黑角、蛇屠箱为红云次级且无练度要求。
- 金属工艺和莱茵科技按技能名称动态枚举；动态成员已拥有时按技能解锁档解析目标。
- 吉星、苍苔、槐琥的同站条件已保存为说明性事实；当前布局无法证明时仍可进入练卡建议，实际排班由 solver 决定。
- 产物数量加权仍为可选策略；当前最新用户裁决要求同层按 17 组顺序，E 阶段不以布局打乱该顺序。

产品映射与风险详见会话中的完整计划讨论；细节以同目录 canonical 与审阅稿为准。
